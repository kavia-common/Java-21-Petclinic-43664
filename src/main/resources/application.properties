# Server (defaults to 8080)
# server.port=8080

# H2 in-memory database configuration
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

spring.datasource.url=jdbc:h2:mem:mybank;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# Ensure data.sql runs after JPA initializes the schema
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always

# Springdoc OpenAPI (Swagger UI available at /swagger-ui.html)
# - OpenAPI JSON: /v3/api-docs
# - Swagger UI:   /swagger-ui.html
